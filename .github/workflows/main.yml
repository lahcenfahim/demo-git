name: "Test Hello Action"
on: [push]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./hello-action@v2
        with:
          name: "Lahcen"
          greeting: "Hey"
